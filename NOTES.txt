- XML Serialization for high score (KEVIN)

- Change the rolls so that only one object can appear at a time (CHUFU)
	- Only roll once for all three objects, instead of three rolls for each object

- Change background images to look nicer (JACKSON)

- Add some type of recursion so that the timer can keep going (ALL) ***